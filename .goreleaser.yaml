# Copyright (c) Joseph Hale, 2026
# SPDX-License-Identifier: MPL-2.0

version: 2

project_name: tooluse-screener

builds:
  - id: tooluse-screener
    main: ./cmd/tooluse-screener
    binary: tooluse-screener
    env:
      - CGO_ENABLED=0
    goos: [linux, darwin, windows]
    goarch: [amd64, arm64]
    flags: [-trimpath]
    ldflags: ["-s -w"]

archives:
  - ids: [tooluse-screener]
    formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]

checksum:
  name_template: checksums.txt

changelog:
  use: github
